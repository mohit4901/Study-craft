from django.test import TestCase

# Create your tests here.

# Example:
# class SignupViewTests(TestCase):
#     def test_signup_success(self):
#         response = self.client.post('/api/signup/', {'email': 'test@example.com', 'password': 'password123'})
#         self.assertEqual(response.status_code, 201)
#         self.assertIn('message', response.json())
#         self.assertEqual(response.json()['message'], 'User created successfully!')
#
#     def test_signup_missing_fields(self):
#         response = self.client.post('/api/signup/', {'email': 'test@example.com'})
#         self.assertEqual(response.status_code, 400)
#         self.assertIn('message', response.json())
#         self.assertEqual(response.json()['message'], 'Email and password are required.')

